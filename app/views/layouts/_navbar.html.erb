<nav class="bg-white border-theme-100 filter drop-shadow-sm border-b-2 w-full h-13 p-1 z-20">
  <div class="flex flex-row justify-end items-center h-7 my-2">
    <div class="flex flex-row items-center">
      <div class="mr-4 w-7 h-7 rounded-full overflow-x-hidden overflow-y-hidden">
        <%= link_to users_info_path(current_user), method: :get do %>
          <%= image_tag(current_user.avatar_url || current_user.imageurl, size:'32x32' , class:'w-full h-full') %>
        <% end %>
      </div>
      <%= link_to icons('house-user', :fas, 'lg', 'pr-5 hover:text-gray-500'), project_path(@project) if @project%>
      <%= link_to icons('th', :fas, 'lg', 'pr-5 hover:text-gray-500'), personal_path %>
      <%= link_to icons('sign-out-alt', :fas, 'lg', 'pr-5 hover:text-gray-500'), destroy_user_session_path, method: :delete %>
    </div>
  </div>
</nav>

