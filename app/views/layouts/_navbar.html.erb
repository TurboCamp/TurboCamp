<nav class="z-50 w-full p-3 bg-white border-b-2 border-theme-100 filter drop-shadow-sm h-17">
  <div class="flex flex-row items-center justify-end my-2 h-7">
    <div class="flex flex-row items-center">
      <%= link_to users_info_path, method: :get do %>
        <% if current_user.avatar.attached? %>  
            <div class="pr-5">
              <%= image_tag(current_user.avatar, size:'32x32' , class:'rounded-full') %>
            </div>
        <% else %>
            <div class="pr-5">
              <%= image_tag('default_user' , size:'32x32' , class:'rounded-full')%>
            </div>
        <% end %>
      <% end %>
      <%= link_to icons('th', :fas, 'lg', 'pr-5'), personal_path, method: :get %>
      <%= link_to icons('house-user', :fas, 'lg', 'pr-5'), project_path(@project) %>
      <%= link_to icons('sign-out-alt', :fas, 'lg', 'pr-5'), destroy_user_session_path, method: :delete %>
    </div>
  </div>
</nav>

